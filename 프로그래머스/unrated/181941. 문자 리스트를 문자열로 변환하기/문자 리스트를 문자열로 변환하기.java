class Solution {
    public String solution(String[] arr) {
        String answer = "";
        for(int s = 0; s < arr.length; s++) {
            answer += arr[s];
        }
        
        return answer;
    }
}